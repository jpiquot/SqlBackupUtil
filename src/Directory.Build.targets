<Project>
	<Target Name="DevOpsVersionTarget" AfterTargets="MinVer">
		<PropertyGroup>
			<BUILD_BUILDID Condition="'$(BUILD_BUILDID)' != ''">$(Version)</BUILD_BUILDID>
		</PropertyGroup>
	</Target>
</Project>
